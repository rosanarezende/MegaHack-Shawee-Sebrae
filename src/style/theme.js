import { createMuiTheme } from "@material-ui/core";

export default createMuiTheme({
  palette: {
    primary: {
      main: '#D12B63',
      contrastText: '#fff'

    },
    secondary: {
      main: "#fff",
      contrastText: '#D12B63',
    }

  }
});